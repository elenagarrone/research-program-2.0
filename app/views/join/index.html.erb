<div class="container-fluid">
    <div class="row">
        <div class="col-sm-12">
            <% if user_signed_in? %>
                <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "float-right" %>
            <% end %>
            <main role="main" class="join">
                <h1 class="h2"><%=t 'join.h1' %></h1>
                <p><%=t 'join.p1' %></p>
                <p><%=t 'join.p2' %></p>
                <a href="#" class="form-access" ><img src="<%= image_path(t 'join.access_form') %>" alt="<%= t 'join.access_alt' %>"></a>
                <p><%=t 'join.p3' %></p>
            </main>
        </div>
    </div>
</div>
